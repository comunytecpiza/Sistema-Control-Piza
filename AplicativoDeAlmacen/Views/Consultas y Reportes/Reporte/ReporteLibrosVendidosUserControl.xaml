<UserControl x:Class="AplicativoDeAlmacen.Views.ReporteLibrosVendidosUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White" FontFamily="Segoe UI">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="📚 Resumen de Libros Vendidos" FontSize="26" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Consulte las estadísticas de ventas por título, colección y rango de fechas." FontSize="14" Foreground="#64748B" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Fecha Inicio:" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <DatePicker Width="150"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Fecha Fin:" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <DatePicker Width="150"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Colección:" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox Width="180"/>
                </StackPanel>

                <Button Content="🔍 Buscar Ventas" Width="130" Height="35" Background="#3B82F6" Foreground="White" FontWeight="Bold" BorderThickness="0" VerticalAlignment="Bottom">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" AutoGenerateColumns="False" Background="White" BorderBrush="#E2E8F0" RowHeight="35" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="CÓDIGO" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="TÍTULO DEL LIBRO" Width="*" IsReadOnly="True" FontWeight="Bold"/>
                <DataGridTextColumn Header="COLECCIÓN" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="CANTIDAD" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="TOTAL (S/)" Width="120" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>