<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="AplicativoDeAlmacen.Views.ImportarCodigos"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AplicativoDeAlmacen.Views"
        mc:Ignorable="d"
        Title="ImportarCodigos" Height="610" Width="851">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Importar codigos" FontSize="16" FontWeight="Bold" Grid.Row="0" Margin="4" />

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,8,0,8">
            <Label Content="Archivo:" VerticalAlignment="Center" />
            <TextBox x:Name="txtRutaArchivo" Width="420" Margin="6,0,6,0" IsReadOnly="True" />
            <Button x:Name="btn_buscar" Content="Seleccionar" Click="Button_Click" Width="100" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel x:Name="pnlLoading" Grid.Row="0" Orientation="Horizontal" Visibility="Collapsed" Margin="0,0,0,6" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="16" />
                <TextBlock Text="  Procesando archivo..." VerticalAlignment="Center" />
            </StackPanel>

            <Border Grid.Row="1" 
        Margin="0,4" 
        BorderBrush="#E5E7EB" 
        BorderThickness="1" 
        CornerRadius="4">
                <DataGrid x:Name="dgDatos"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              IsReadOnly="False"
              EnableRowVirtualization="True"
              EnableColumnVirtualization="True"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              ScrollViewer.CanContentScroll="True"
              VerticalScrollBarVisibility="Auto"
              HorizontalScrollBarVisibility="Auto">

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding EstadoValido}" Value="True">
                                    <Setter Property="Background" Value="#E6F4EA" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding EstadoValido}" Value="False">
                                    <Setter Property="Background" Value="#FDEAEA" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding RowNumber}" Width="50" IsReadOnly="True" />
                        <DataGridTextColumn Header="Codigo" Binding="{Binding CodigoRaw}" Width="*" IsReadOnly="True" />
                        <DataGridTextColumn Header="Normalizado" Binding="{Binding CodigoNorm}" Width="*" IsReadOnly="True" />
                        <DataGridCheckBoxColumn Header="Encontrado" Binding="{Binding Encontrado, Mode=TwoWay}" Width="90" />
                        <DataGridTextColumn Header="Producto" Binding="{Binding ProductoDesc}" Width="2*" IsReadOnly="True" />
                        <DataGridTextColumn Header="Estado" Binding="{Binding EstadoNombre}" Width="120" IsReadOnly="True" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,6,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                <Label Content="Total codigos:" VerticalAlignment="Center" />
                <TextBox x:Name="txtTotalCodigos" Width="80" IsReadOnly="True" Margin="6,0,12,0" />
                <Label Content="Validos:" VerticalAlignment="Center" />
                <TextBox x:Name="txtValidos" Width="60" IsReadOnly="True" Margin="6,0,12,0" />
                <Label Content="Invalidos:" VerticalAlignment="Center" />
                <TextBox x:Name="txtInvalidos" Width="60" IsReadOnly="True" Margin="6,0,12,0" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="8,0,12,0">
                <CheckBox x:Name="chkIncluirInvalidos" Content="Incluir codigos invalidos" VerticalAlignment="Center" ToolTip="Si esta marcado, incluir codigos aunque su estado no coincida con el motivo" Visibility="Collapsed" />
                <Button x:Name="btnExportInvalid" Content="Exportar invalidos" Click="BtnExportInvalid_Click" Width="120" Margin="8,0,0,0" />
            </StackPanel>

            <Button x:Name="btnTransferir" Content="Transferir" Click="Button_Click_2" Width="120" Margin="6,0,6,0" IsEnabled="False" />
            <Button x:Name="btnSalir" Content="Salir" Click="Button_Click_1" Width="100" />
        </StackPanel>
    </Grid>
</Window>
