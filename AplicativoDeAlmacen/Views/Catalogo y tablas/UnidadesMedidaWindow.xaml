<UserControl x:Class="AplicativoDeAlmacen.Views.UnidadesMedidaUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F3F4F6"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <!-- Estilos unificados (Iguales a los de Productos) -->
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TÍTULO PRINCIPAL -->
        <TextBlock Text="📏 Unidades de Medida" FontSize="26" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,20"/>

        <!-- BARRA DE HERRAMIENTAS (BÚSQUEDA Y BOTONES) -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🔍 Buscar:" VerticalAlignment="Center" Foreground="#4B5563" FontWeight="SemiBold" Margin="0,0,10,0"/>
                <TextBox x:Name="UnidadSearchBox" Width="250" FontSize="14" ToolTip="Buscar unidad..."/>
                <Button Content="Buscar" Click="UnidadSearchButton_Click" Background="#3B82F6" Margin="10,0,0,0" MinWidth="90"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="+ Agregar" Click="AddUnidadButton_Click" Background="#10B981" Margin="0,0,10,0" MinWidth="110"/>
                <Button Content="✏️ Editar" Click="EditUnidadButton_Click" Background="#F59E0B" MinWidth="110"/>
            </StackPanel>
        </Grid>

        <!-- TABLA DE DATOS (DATAGRID) -->
        <Border Grid.Row="2" CornerRadius="8" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" Padding="1">
            <DataGrid x:Name="UnidadGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                      Background="Transparent" BorderThickness="0" 
                      RowHeight="35" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F3F4F6">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F9FAFB"/>
                        <Setter Property="Foreground" Value="#374151"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Código" Binding="{Binding Id}" Width="80"/>
                    <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*"/>
                    <DataGridTextColumn Header="Abreviatura" Binding="{Binding Abreviatura}" Width="150"/>
                    <!-- Asumo que luego conectaremos esto con tu objeto Estado -->
                    <DataGridTextColumn Header="Estado" Binding="{Binding Estado.Nombre}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- MODAL OSCURO DE FONDO -->
        <Grid x:Name="AddEditUnidadModal" Visibility="Collapsed" Background="#80000000" Grid.RowSpan="3" Margin="-20">
            <!-- VENTANA DEL MODAL DE AGREGAR/EDITAR -->
            <Border Background="White" Width="400" VerticalAlignment="Center" HorizontalAlignment="Center" CornerRadius="12" Padding="30">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="25" ShadowDepth="5" Opacity="0.3" Direction="270"/>
                </Border.Effect>

                <StackPanel>
                    <!-- TÍTULO DEL MODAL -->
                    <TextBlock x:Name="ModalTitle" Text="Nueva Unidad" FontSize="22" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15" TextAlignment="Center"/>
                    <Border Height="1" Background="#E5E7EB" Margin="0,0,0,20"/>

                    <TextBlock Text="Descripción" Foreground="#4B5563" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtDescripcionUnidad" Margin="0,0,0,15" Height="30"/>

                    <TextBlock Text="Abreviatura" Foreground="#4B5563" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtAbreviatura" Margin="0,0,0,15" Height="30"/>

                    <TextBlock Text="Estado" Foreground="#4B5563" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="CmbEstadoUnidad" Margin="0,0,0,25" Height="30"/>

                    <Border Height="1" Background="#E5E7EB" Margin="0,0,0,20"/>

                    <!-- BOTONES DE ACCIÓN DEL MODAL -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancelar" Click="CancelarAddEditUnidad_Click" Background="#6B7280" Margin="0,0,10,0" Width="100"/>
                        <Button Content="Guardar" Click="GuardarUnidad_Click" Background="#3B82F6" Width="120"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>