<UserControl x:Class="AplicativoDeAlmacen.Views.RegistroCodigosUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F3F4F6"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="📝 Registro de Códigos" FontSize="26" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Background="White" Margin="0,0,15,0">
                <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Padding="10,5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Colección:" VerticalAlignment="Center" Foreground="#4B5563" FontWeight="SemiBold" Margin="0,0,10,0"/>
                        <ComboBox x:Name="CmbFiltroColeccion" Width="100" SelectionChanged="Filtros_Changed"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Padding="15,5">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <RadioButton x:Name="RbLibroGuia" Content="Libros Guía" IsChecked="True" Checked="Filtros_Changed" Foreground="#4B5563" FontWeight="SemiBold" Margin="0,0,15,0" VerticalAlignment="Center"/>
                        <RadioButton x:Name="RbLibroVenta" Content="Libros Venta" Checked="Filtros_Changed" Foreground="#4B5563" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="+ Nuevo Registro" Click="BtnNuevo_Click" Background="#10B981" Margin="0,0,10,0" MinWidth="140"/>
                <Button Content="🗑️ Eliminar" Click="BtnEliminar_Click" Background="#EF4444" MinWidth="110"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" CornerRadius="8" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" Padding="1">
            <DataGrid x:Name="CodigosDataGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                      Background="Transparent" BorderThickness="0" 
                      RowHeight="35" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F3F4F6">

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick"/>
                        <Setter Property="Cursor" Value="Hand"/>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F9FAFB"/>
                        <Setter Property="Foreground" Value="#374151"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Producto" Binding="{Binding Producto.Descripcion}" Width="*"/>
                    <DataGridTextColumn Header="U. Medida" Binding="{Binding Producto.UnidadMedida.Descripcion}" Width="100"/>
                    <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="80"/>
                    <DataGridTextColumn Header="Desde" Binding="{Binding Desde}" Width="140"/>
                    <DataGridTextColumn Header="Hasta" Binding="{Binding Hasta}" Width="140"/>

                    <DataGridTemplateColumn Header="Acciones" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="👁️ Detalle" Click="BtnVerDetalle_Click" Background="#3B82F6" 
                                        Foreground="White" FontWeight="Bold" Padding="8,2" Margin="2" Cursor="Hand"
                                        BorderThickness="0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid x:Name="ModalAgregar" Visibility="Collapsed" Background="#80000000" Grid.RowSpan="3" Margin="-20">
            <Border Background="White" Width="500" VerticalAlignment="Center" HorizontalAlignment="Center" CornerRadius="12" Padding="30">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="25" ShadowDepth="5" Opacity="0.3" Direction="270"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Agregar Nuevos Códigos" FontSize="22" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15" TextAlignment="Center"/>
                    <Border Height="1" Background="#E5E7EB" Margin="0,0,0,20"/>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Colección:" Foreground="#4B5563" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbModalColeccion"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Categoría:" Foreground="#4B5563" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbModalCategoria"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Producto:" Foreground="#4B5563" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="CmbProducto" IsEditable="True" IsTextSearchEnabled="True" 
                              DisplayMemberPath="Descripcion" TextSearch.TextPath="Descripcion"
                              TextBoxBase.TextChanged="CmbProducto_TextChanged" SelectionChanged="CmbProducto_SelectionChanged"
                              Margin="0,0,0,15"/>

                    <TextBlock Text="Cantidad de códigos a generar:" Foreground="#4B5563" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtCantidad" TextChanged="TxtCantidad_TextChanged" Margin="0,0,0,15" Height="35"/>

                    <Grid Margin="0,0,0,25">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Desde:" Foreground="#4B5563" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtDesde" IsReadOnly="True" Background="#F3F4F6"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Hasta:" Foreground="#4B5563" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtHasta" IsReadOnly="True" Background="#F3F4F6"/>
                        </StackPanel>
                    </Grid>

                    <Border Height="1" Background="#E5E7EB" Margin="0,0,0,20"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancelar" Click="BtnCancelar_Click" Background="#6B7280" Margin="0,0,10,0" Width="100"/>
                        <Button Content="💾 Grabar Códigos" Click="BtnGuardar_Click" Background="#3B82F6" Width="150"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>