<Window x:Class="AplicativoDeAlmacen.Views.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Procesando Operación Masiva" 
        Height="180" Width="450"
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent">

    <Border Background="White" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Color="#000000" Opacity="0.15"/>
        </Border.Effect>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="txtTitulo" Text="Procesando datos..." FontSize="16" FontWeight="Bold" Foreground="#1E293B" Grid.Row="0" Margin="0,0,0,8"/>

            <TextBlock x:Name="txtEstado" Text="Por favor espere, no cierre la aplicación..." FontSize="13" Foreground="#64748B" Grid.Row="1" Margin="0,0,0,16"/>

            <Grid Grid.Row="2" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ProgressBar x:Name="pbProgreso" Height="10" Minimum="0" Maximum="100" Value="0" Foreground="#4F46E5" Background="#F1F5F9"/>
                <TextBlock x:Name="txtPorcentaje" Grid.Row="1" Text="0%" HorizontalAlignment="Right" FontSize="11" FontWeight="SemiBold" Foreground="#4F46E5" Margin="0,4,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>