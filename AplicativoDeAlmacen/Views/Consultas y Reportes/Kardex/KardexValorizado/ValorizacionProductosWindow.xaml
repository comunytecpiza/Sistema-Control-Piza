<Window x:Class="AplicativoDeAlmacen.Views.Consultas_y_Reportes.Consulta.ValorizacionProductosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Valorización de Productos" Height="240" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="#F3F4F6" FontFamily="Segoe UI">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🔄 Valorización de Productos" FontSize="16" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,10"/>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15">
            <StackPanel VerticalAlignment="Center">
                <!-- Desde -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Desde:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <ComboBox x:Name="CboMesDesde" Grid.Column="1" Margin="5,0"/>
                    <TextBox x:Name="TxtAnioDesde" Grid.Column="2" TextAlignment="Center" VerticalContentAlignment="Center"/>
                </Grid>

                <!-- Hasta -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Hasta:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <ComboBox x:Name="CboMesHasta" Grid.Column="1" Margin="5,0"/>
                    <TextBox x:Name="TxtAnioHasta" Grid.Column="2" TextAlignment="Center" VerticalContentAlignment="Center"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Botones -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="⚙️ Ejecutar" Width="100" Height="35" Background="#10B981" Foreground="White" FontWeight="Bold" Cursor="Hand" Click="BtnEjecutar_Click" Margin="0,0,10,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Content="🚪 Salir" Width="90" Height="35" Background="#9CA3AF" Foreground="White" FontWeight="Bold" Cursor="Hand" Click="BtnSalir_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>