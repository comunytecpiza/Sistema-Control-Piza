<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="AplicativoDeAlmacen.Views.ImportarCodigos"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Auditoría e Importación Inteligente de Lotes" Height="680" Width="1050" WindowStartupLocation="CenterOwner" Background="#F8FAFC">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="🚀 Asistente de Importación y Mapeo de Códigos" FontSize="22" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,15" Foreground="#1E293B" />

        <!-- ARCHIVO Y HEALTH SCORE -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Archivo:" VerticalAlignment="Center" FontWeight="Bold" Foreground="#475569" Margin="0,0,10,0"/>
                    <TextBox x:Name="txtRutaArchivo" Width="450" Height="35" IsReadOnly="True" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#CBD5E1"/>
                    <Button x:Name="btn_buscar" Content="📂 Explorar" Click="Button_Click" Width="110" Height="35" Margin="10,0,0,0" Background="#3B82F6" Foreground="White" FontWeight="Bold" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <!-- INDICADOR DE SALUD -->
                <Border Grid.Column="1" x:Name="brdHealth" Background="#F1F5F9" CornerRadius="4" Padding="15,5" BorderBrush="#E2E8F0" BorderThickness="1">
                    <TextBlock x:Name="txtHealth" Text="Esperando archivo..." FontWeight="Bold" Foreground="#64748B" VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>

        <!-- BUSCADOR Y ALERTAS -->
        <Border Grid.Row="2" Background="#F1F5F9" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Column="0" Margin="0,0,20,0">
                    <TextBlock Text="Filtro de Colección:" VerticalAlignment="Center" FontWeight="Bold" Foreground="#475569" Margin="0,0,10,0"/>
                    <ComboBox x:Name="cboFiltroTipoLibro" Width="160" Height="35" VerticalContentAlignment="Center" SelectedIndex="0" SelectionChanged="CboFiltroTipoLibro_SelectionChanged">
                        <ComboBoxItem Content="🔍 Mostrar Todos" />
                        <ComboBoxItem Content="📘 Solo Guía" />
                        <ComboBoxItem Content="📙 Solo Venta" />
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Column="1">
                    <TextBlock Text="Búsqueda Rápida:" VerticalAlignment="Center" FontWeight="Bold" Foreground="#475569" Margin="0,0,10,0"/>
                    <TextBox x:Name="txtBuscarCodigo" Width="220" Height="35" Padding="8,0" VerticalContentAlignment="Center" TextChanged="TxtBuscarCodigo_TextChanged"/>
                </StackPanel>

                <Button x:Name="btnVerDuplicados" Grid.Column="3" Click="BtnVerDuplicados_Click" Background="#FEF3C7" BorderBrush="#F59E0B" BorderThickness="1" Height="35" Padding="15,0" Cursor="Hand" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚠️ Filtrar " Foreground="#D97706" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtContadorDuplicados" Text="0" Foreground="#B45309" FontWeight="Black" VerticalAlignment="Center"/>
                        <TextBlock Text=" colisiones internas" Foreground="#D97706" FontWeight="Bold" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- GRILLA AUDITADA (Modo Solo Lectura) -->
        <Border Grid.Row="3" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Background="White" ClipToBounds="True">
            <DataGrid x:Name="dgDatos" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" BorderThickness="0"
                      EnableRowVirtualization="True" EnableColumnVirtualization="True" RowHeight="35"
                      VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding EstadoValido}" Value="False">
                                <Setter Property="Background" Value="#FEF2F2" />
                                <Setter Property="Foreground" Value="#991B1B"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding EsClonDuplicado}" Value="True">
                                <Setter Property="Background" Value="#FEF3C7" />
                                <Setter Property="Foreground" Value="#92400E"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Foreground" Value="#475569"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding RowNumber}" Width="50" IsReadOnly="True" />
                    <DataGridTextColumn Header="Código Extraído" Binding="{Binding CodigoRaw}" Width="180" IsReadOnly="True" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Producto Relacionado" Binding="{Binding ProductoDesc}" Width="2*" IsReadOnly="True" />
                    <DataGridTextColumn Header="Diagnóstico del Sistema" Binding="{Binding ObservacionAuditoria}" Width="250" IsReadOnly="True" />
                    <DataGridTextColumn Header="Colección Base" Binding="{Binding TipoGuiaBD}" Width="130" IsReadOnly="True" FontWeight="Bold"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- TOTALES Y ACCIONES -->
        <Grid Grid.Row="4" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                <Border Background="#F1F5F9" Padding="15,8" CornerRadius="6" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Total:" FontWeight="Bold" Foreground="#475569" Margin="0,0,5,0"/>
                        <TextBlock x:Name="txtTotalCodigos" Text="0" FontWeight="Black"/>
                    </StackPanel>
                </Border>
                <Border Background="#ECFDF5" Padding="15,8" CornerRadius="6" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Aptos:" FontWeight="Bold" Foreground="#065F46" Margin="0,0,5,0"/>
                        <TextBlock x:Name="txtValidos" Text="0" FontWeight="Black" Foreground="#047857"/>
                    </StackPanel>
                </Border>
                <Border Background="#FEF2F2" Padding="15,8" CornerRadius="6" Margin="0,0,15,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Ignorados/Errores:" FontWeight="Bold" Foreground="#991B1B" Margin="0,0,5,0"/>
                        <TextBlock x:Name="txtInvalidos" Text="0" FontWeight="Black" Foreground="#B91C1C"/>
                    </StackPanel>
                </Border>
                <Button x:Name="btnExportInvalid" Content="📥 Exportar Reporte de Errores" Click="BtnExportInvalid_Click" Height="35" Padding="15,0" Background="#EF4444" Foreground="White" FontWeight="Bold" Cursor="Hand" Visibility="Collapsed"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center">
                <Button x:Name="btnSalir" Content="Cancelar" Click="Button_Click_1" Width="100" Height="40" Background="Transparent" Foreground="#64748B" FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand"/>
                <Button x:Name="btnTransferir" Content="Transferir Lote" Click="Button_Click_2" Width="200" Height="40" IsEnabled="False" Background="#10B981" Foreground="White" FontWeight="Bold" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>