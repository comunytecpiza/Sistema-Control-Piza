<Window x:Class="AplicativoDeAlmacen.Views.PersonasComercialesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AplicativoDeAlmacen.Views"
        Title="Catálogo de Personas Comerciales" 
        Height="750" Width="1250"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6F9"
        FontFamily="Segoe UI">

    <Window.Resources>
        <local:TipoPersonaToIsReadOnlyConverter x:Key="TipoPersonaToIsReadOnlyConverter"/>

        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" 
                                Padding="{TemplateBinding Padding}" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4A5568"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CBD5E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="White" BorderBrush="#CBD5E0" BorderThickness="1" CornerRadius="4">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="15,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Filtrar por:" Margin="0,0,15,0" FontWeight="SemiBold" Foreground="#4A5568"/>
                    <RadioButton Content="Razón Social" IsChecked="True" Margin="0,0,15,0" Checked="FilterRadioButton_Checked"/>
                    <RadioButton Content="Nombre Comercial" Margin="0,0,15,0" Checked="FilterRadioButton_Checked"/>
                    <RadioButton Content="R.U.C." Margin="0,0,15,0" Checked="FilterRadioButton_Checked"/>
                    <RadioButton Content="D.N.I." Margin="0,0,15,0" Checked="FilterRadioButton_Checked"/>
                    <TextBox x:Name="SearchTextBox" Width="280" Margin="10,0,0,0" TextChanged="SearchTextBox_TextChanged"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Column="1">
                    <Button Content="+ Agregar Persona" Click="AddButton_Click" Style="{StaticResource RoundedButtonStyle}" Background="#2B6CB0" Foreground="White" Width="160" Height="38" Margin="0,0,10,0"/>
                    <Button x:Name="EditButton" Content="Editar Registro" Click="EditButton_Click" Style="{StaticResource RoundedButtonStyle}" Background="#EDF2F7" Foreground="#4A5568" Width="140" Height="38"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="20" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1">
            <DataGrid x:Name="PersonasDataGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                      SelectionChanged="PersonasDataGrid_SelectionChanged" Background="Transparent" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Código" Binding="{Binding Id}" Width="70"/>
                    <DataGridTextColumn Header="Razón Social" Binding="{Binding RazonSocial}" Width="2*"/>
                    <DataGridTextColumn Header="Nombre Comercial" Binding="{Binding NombreComercial}" Width="2*"/>
                    <DataGridTextColumn Header="T. Persona" Binding="{Binding TipoPersona.Nombre}" Width="100"/>
                    <DataGridTextColumn Header="R.U.C." Binding="{Binding Ruc}" Width="110"/>
                    <DataGridTextColumn Header="D.N.I." Binding="{Binding Dni}" Width="90"/>
                    <DataGridTextColumn Header="Localidad" Binding="{Binding Localidad.Nombre}" Width="1.2*"/>
                    <DataGridTextColumn Header="Zona Promotoria" Binding="{Binding ZonaPromotoria.Descripcion}" Width="1.2*"/>
                    <DataGridTextColumn Header="Estado" Binding="{Binding Estado.Nombre}" Width="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid x:Name="ModalBackground" Visibility="Collapsed" Background="#A0000000" Grid.RowSpan="2">
            <Border Background="White" CornerRadius="8" Width="700" Height="650" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="25">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="ModalTitle" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

                    <ScrollViewer Grid.Row="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0">
                                <TextBlock Text="Tipo Persona" Style="{StaticResource FormLabelStyle}"/>
                                <ComboBox x:Name="TipoPersonaComboBox" SelectionChanged="TipoPersonaComboBox_SelectionChanged"/>
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="2">
                                <TextBlock Text="Razón Social" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox x:Name="RazonSocialTextBox" IsReadOnly="{Binding ElementName=TipoPersonaComboBox, Path=SelectedItem.Content, Converter={StaticResource TipoPersonaToIsReadOnlyConverter}}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0">
                                <TextBlock Text="Ap. Paterno" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox x:Name="ApellidoPaternoTextBox"/>
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="2">
                                <TextBlock Text="Ap. Materno" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox x:Name="ApellidoMaternoTextBox"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0">
                                <TextBlock Text="Nombres" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox x:Name="NombresTextBox"/>
                            </StackPanel>
                            <StackPanel Grid.Row="2" Grid.Column="2">
                                <TextBlock Text="Nombre Comercial" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox x:Name="NombreComercialTextBox"/>
                            </StackPanel>

                            <StackPanel Grid.Row="3" Grid.Column="0">
                                <TextBlock Text="R.U.C." Style="{StaticResource FormLabelStyle}"/>
                                <TextBox x:Name="RucTextBox" MaxLength="11"/>
                            </StackPanel>
                            <StackPanel Grid.Row="3" Grid.Column="2">
                                <TextBlock Text="D.N.I." Style="{StaticResource FormLabelStyle}"/>
                                <TextBox x:Name="DniTextBox" MaxLength="8"/>
                            </StackPanel>

                            <StackPanel Grid.Row="4" Grid.Column="0" Orientation="Horizontal" Margin="0,10">
                                <CheckBox x:Name="DireccionFiscalCheckBox" Content="Dirección Fiscal" Checked="DireccionFiscalCheckBox_Checked" Unchecked="DireccionFiscalCheckBox_Unchecked"/>
                            </StackPanel>
                            <StackPanel Grid.Row="4" Grid.Column="2" Orientation="Horizontal" Margin="0,10">
                                <CheckBox x:Name="InstitucionEducativaCheckBox" Content="Inst. Educativa" Checked="InstitucionEducativaCheckBox_Checked" Unchecked="InstitucionEducativaCheckBox_Unchecked"/>
                            </StackPanel>

                            <StackPanel Grid.Row="5" Grid.ColumnSpan="3">
                                <TextBlock Text="Dirección" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox x:Name="DireccionTextBox"/>
                            </StackPanel>

                            <StackPanel Grid.Row="6" Grid.Column="0">
                                <TextBlock Text="Departamento" Style="{StaticResource FormLabelStyle}"/>
                                <ComboBox x:Name="DepartamentoComboBox" SelectionChanged="DepartamentoComboBox_SelectionChanged"/>
                            </StackPanel>
                            <StackPanel Grid.Row="6" Grid.Column="2">
                                <TextBlock Text="Provincia" Style="{StaticResource FormLabelStyle}"/>
                                <ComboBox x:Name="ProvinciaComboBox" SelectionChanged="ProvinciaComboBox_SelectionChanged"/>
                            </StackPanel>

                            <StackPanel Grid.Row="7" Grid.Column="0">
                                <TextBlock Text="Distrito" Style="{StaticResource FormLabelStyle}"/>
                                <ComboBox x:Name="DistritoComboBox"/>
                            </StackPanel>
                            <StackPanel Grid.Row="7" Grid.Column="2">
                                <TextBlock Text="Localidad" Style="{StaticResource FormLabelStyle}"/>
                                <ComboBox x:Name="LocalidadComboBox"/>
                            </StackPanel>

                            <StackPanel Grid.Row="8" Grid.Column="0">
                                <TextBlock Text="Zona Promotoria" Style="{StaticResource FormLabelStyle}"/>
                                <ComboBox x:Name="ZonaPromotoriaComboBox"/>
                            </StackPanel>
                            <StackPanel Grid.Row="8" Grid.Column="2">
                                <TextBlock Text="Estado" Style="{StaticResource FormLabelStyle}"/>
                                <ComboBox x:Name="EstadoComboBox"/>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                        <Button Content="Cancelar" Click="CancelButton_Click" Style="{StaticResource RoundedButtonStyle}" Width="100" Margin="0,0,10,0"/>
                        <Button Content="Guardar" Click="SaveButton_Click" Style="{StaticResource RoundedButtonStyle}" Background="#2B6CB0" Foreground="White" Width="110"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>